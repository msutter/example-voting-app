apiVersion: v1
kind: Service
metadata:
  annotations:
    kubectl.kubernetes.io/last-applied-configuration: |
      {"apiVersion":"v1","kind":"Service","metadata":{"annotations":{},"labels":{"app":"db","app.kubernetes.io/managed-by":"tilt"},"name":"db","namespace":"vote-tilt"},"spec":{"ports":[{"name":"db-service","port":5432,"targetPort":5432}],"selector":{"app":"db"},"type":"ClusterIP"}}
  labels:
    app: db
    app.kubernetes.io/managed-by: tilt
    chart: '{{.Chart.Name}}-{{.Chart.Version}}'
    heritage: '{{.Release.Service}}'
    release: '{{.Release.Name}}'
  name: '{{ template "fullname" . }}-db'
  namespace: '{{.Values.db.namespace}}'
spec:
  ports:
  - name: db-service
    port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: '{{.Release.Name}}-db'
  sessionAffinity: '{{.Values.db.sessionAffinity}}'
  type: '{{.Values.db.serviceType}}'
