apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgresql
  name: postgresql
  namespace: vote-tilt
spec:
  type: ClusterIP
  ports:
  - name: "postgresql-service"
    port: 5432
    targetPort: 5432
  selector:
    app: postgresql
  
